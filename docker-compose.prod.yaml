version: '3'

services:
  transolyapp: # cambiar nombre
    depends_on:
      - db
    build:
      context: .
      dockerfile: Dockerfile
    image: transoly-docker # cambiar nombre
    container_name: transolyapp # cambiar nombre
    restart: always # reiniciar el contenedor si se detiene
    ports:
      - '${PORT}:${PORT}'
    # working_dir: /var/www/transoly
    environment:
      MONGODB: ${MONGODB}
      PORT: ${PORT}
    # volumes:
    #   - ./:/var/www/transoly

  #db:
  #  image: mongo:5
  #  container_name: mongo-poke # cambiar nombre
  #  restart: always
  #  ports:
  #    - 27017:27017
  #  environment:
  #    MONGODB_DATABASE: nest-pokemon # cambiar nombre
  #  volumes:
  #    - ./mongo:/data/db

  db:
    image: postgres:14.3
    container_name: transolyfer_db_container
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - ./postgres:/var/lib/postgresql/data
